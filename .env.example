FLASK_ENV=production
FLASK_APP=app.py
PORT=5000

HEADLESS_MODE=true
MIN_DELAY=2
MAX_DELAY=5

# Si subes el JSON como archivo en el servidor:
GOOGLE_CREDENTIALS_FILE=credentials.json

# Alternativa segura: credenciales en base64 (no versionar)
# GOOGLE_CREDENTIALS_BASE64=eyJh...
